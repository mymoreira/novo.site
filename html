<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Texto sobre Imagem</title>
    <style>
        /* Contêiner da imagem */
        .imagem-container {
            position: relative;
            width: 100%;
            max-width: 600px; /* Largura máxima da imagem */
        }

        /* A imagem de fundo */
        .imagem-container img {
            width: 100%;
            height: auto;
            display: block; /* Para remover espaço abaixo da imagem */
        }

        /* Texto que ficará sobre a imagem */
        .texto {
            position: absolute;
            top: 50%; /* Posiciona o texto no centro vertical */
            left: 50%; /* Posiciona o texto no centro horizontal */
            transform: translate(-50%, -50%); /* Ajusta para o centro exato */
            color: white; /* Cor do texto */
            font-size: 2em; /* Tamanho do texto */
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7); /* Sombra no texto para destaque */
            opacity: 0.8;
            pointer-events: none; /* Impede que o texto atrapalhe a interação com outros elementos */
        }

        /* Animação para o movimento do texto */
        .texto:hover {
            animation: moverTexto 3s infinite alternate;
        }

        /* Animação de movimento */
        @keyframes moverTexto {
            0% {
                top: 50%;
                left: 50%;
            }
            100% {
                top: 40%;
                left: 60%;
            }
        }
    </style>
</head>
<body>

    <div class="imagem-container">
        <img src="https://via.placeholder.com/600x400" alt="Imagem Exemplo">
        <div class="texto">Texto sobre a Imagem</div>
    </div>

</body>
</html>
